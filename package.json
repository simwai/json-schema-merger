{
  "name": "json-schema-merger",
  "version": "0.1.0",
  "description": "POC: Draft 2020-12 local JSON Schema merger with runtime meta-schema keyword discovery",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "got": "^14.4.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
